
apiVersion: v1
kind: Service
metadata:
  name: spring-service
  namespace: devops     # ✅ Add this line
  
spec:
  type: NodePort
  selector:
    app: student-app
  ports:
    - port: 8089
      targetPort: 8089
      nodePort: 30080

